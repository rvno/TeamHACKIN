<%= form_for new_file_path do |f| %>
  <%= f.text_field "url",
      placeholder: "github url",
      pattern: ".*github.com.*",
      oninvalid: "setCustomValidity('Plz enter on Alphabets ')",
      onchange: "try{setCustomValidity('')}catch(e){}" %>
<% end %>
